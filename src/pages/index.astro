---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Nightshift Tower Defense User Guide">
  <section class="home-bleed">
    <div class="home-bg"></div>
    <div class="home-overlay"></div>

    <div class="home-inner">
      <div class="home-card">
        <div class="home-kicker">Official community guide</div>

        <h1 class="home-title">Nightshift Tower Defense User Guide</h1>

        <p class="home-text">
          Hey, I’m <strong>Clancy</strong>. This user guide is basically just to help you all understand all the nerdy facts.
          It’s here to make the numbers easy to understand, keep tier lists and unit info in one place, and stay updated as the game changes.
        </p>

        <div class="home-divider"></div>

        <h2 class="home-subtitle">Community</h2>

        <p class="home-text">
          Want to help improve the guide, report mistakes, or just hang out with the community? Join us here:
        </p>

        <a class="home-button" href="https://discord.gg/YOUR_INVITE" target="_blank" rel="noopener">
          Join the NTD Discord
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Full-bleed section that stretches across the whole page width */
  .home-bleed{
    position: relative;
    left: 50%;
    right: 50%;
    width: 100vw;
    margin-left: -50vw;
    margin-right: -50vw;

    min-height: calc(100vh - 110px); /* leaves room for the sticky top bar */
    overflow: hidden;
    border-top: 1px solid var(--border);
  }

  /* Background image layer */
  .home-bg{
    position:absolute;
    inset:0;
    background-image: url("/images/home-bg.png");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;

    /* Makes it feel “in the background” */
    filter: blur(10px) brightness(0.75) saturate(1.05);
    transform: scale(1.06);
  }

  /* Readability overlay */
  .home-overlay{
    position:absolute;
    inset:0;
    background:
      radial-gradient(1100px 700px at 18% 20%, rgba(106,168,255,.18), transparent 60%),
      radial-gradient(900px 700px at 80% 25%, rgba(159,91,255,.12), transparent 55%),
      linear-gradient(180deg, rgba(11,14,20,.65), rgba(11,14,20,.90));
  }

  /* Centered content area */
  .home-inner{
    position: relative;
    z-index: 1;
    max-width: 1180px;
    margin: 0 auto;
    padding: 54px 18px 80px;
    display:flex;
    align-items:center;
    justify-content:center;
  }

  /* The readable card on top */
  .home-card{
    width: 100%;
    max-width: 860px;
    border: 1px solid var(--border);
    border-radius: 22px;
    background: rgba(0,0,0,.42);
    box-shadow: var(--shadow);
    padding: 28px 24px;
  }

  .home-kicker{
    display:inline-block;
    padding: 6px 12px;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: rgba(255,255,255,.04);
    color: var(--muted);
    font-size: 12px;
    letter-spacing: .08em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  .home-title{
    margin: 0;
    font-size: clamp(32px, 4.6vw, 54px);
    line-height: 1.05;
    letter-spacing: -0.02em;
  }

  .home-subtitle{
    margin: 0;
    margin-top: 14px;
    font-size: 18px;
    letter-spacing: -0.01em;
  }

  .home-text{
    margin-top: 14px;
    margin-bottom: 0;
    color: var(--muted);
    line-height: 1.7;
    font-size: 16px;
  }

  .home-divider{
    height: 1px;
    background: var(--border);
    margin: 18px 0;
  }

  .home-button{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    margin-top: 14px;
    padding: 12px 14px;
    border-radius: 14px;
    border: 1px solid var(--border);
    background: rgba(255,255,255,.04);
    text-decoration:none;
    font-weight: 800;
  }

  .home-button:hover{
    border-color: rgba(106,168,255,.6);
    background: rgba(106,168,255,.10);
    color: var(--text);
  }
</style>
