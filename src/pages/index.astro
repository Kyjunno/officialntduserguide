---
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout title="Nightshift Tower Defense User Guide">
  <section class="home-hero">
    <div class="home-bg"></div>
    <div class="home-overlay"></div>

    <div class="home-inner">
      <div class="home-card">
        <div class="home-kicker">Official Community Guide</div>

        <h1 class="home-title">Nightshift Tower Defense<br />User Guide</h1>

        <p class="home-text">
          Hey there, I’m <strong>Clancy</strong>. This user guide is basically here to help you understand all the nerdy facts.
          It keeps unit info, tier lists, and mechanics in one place, and stays updated as the game changes.
        </p>

        <div class="home-divider"></div>

        <h2 class="home-subtitle">Community</h2>

        <p class="home-text">
          Want to help improve the guide, report mistakes, or just hang out with the community? Join us here:
        </p>

        <a class="home-button" href="https://discord.gg/XHXjcVA4cw" target="_blank" rel="noopener">
          Join the NTD Discord
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Turn OFF the normal page container behavior on the Home page only */
  :global(main.shell){
    max-width: none;
    padding: 0;
  }

  /* Optional: kill any accidental horizontal overflow site-wide */
  :global(html), :global(body){
    overflow-x: hidden;
  }

  /* Full-bleed hero under the top bar */
  .home-hero{
    position: relative;
    width: 100%;
    height: calc(100vh - 72px); /* height of your sticky topbar */
    overflow: hidden;
  }

  .home-bg{
    position:absolute;
    inset:0;
    background-image: url("/images/home-bg.png");
    background-repeat: no-repeat;
    background-size: cover;

    /* Clean default framing. No wild shift. */
    background-position: center;

    filter: brightness(0.60) saturate(1.05) blur(3px);
    transform: scale(1.05);
  }

  .home-overlay{
    position:absolute;
    inset:0;
    background:
      radial-gradient(900px 600px at 22% 30%, rgba(106,168,255,.18), transparent 60%),
      radial-gradient(900px 700px at 78% 30%, rgba(159,91,255,.14), transparent 55%),
      linear-gradient(180deg, rgba(11,14,20,.55), rgba(11,14,20,.92));
  }

  .home-inner{
    position: relative;
    z-index: 1;
    height: 100%;
    max-width: 1180px;
    margin: 0 auto;
    padding: 56px 18px;
    display:flex;
    align-items:center;
    justify-content:center;
  }

  .home-card{
    width: 100%;
    max-width: 860px;
    border: 1px solid var(--border);
    border-radius: 22px;
    background: rgba(0,0,0,.48);
    box-shadow: var(--shadow);
    padding: 30px 26px;
  }

  .home-kicker{
    display:inline-block;
    padding: 6px 12px;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: rgba(255,255,255,.05);
    color: var(--muted);
    font-size: 12px;
    letter-spacing: .08em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  .home-title{
    margin:0;
    font-size: clamp(34px, 4.8vw, 56px);
    line-height: 1.05;
    letter-spacing: -0.02em;
  }

  .home-subtitle{
    margin-top:16px;
    font-size:18px;
  }

  .home-text{
    margin-top:14px;
    color: var(--muted);
    line-height: 1.7;
    font-size: 16px;
  }

  .home-divider{
    height: 1px;
    background: var(--border);
    margin: 20px 0;
  }

  .home-button{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    margin-top:16px;
    padding:12px 16px;
    border-radius:14px;
    border:1px solid var(--border);
    background: rgba(255,255,255,.05);
    font-weight: 800;
    text-decoration:none;
  }

  .home-button:hover{
    border-color: rgba(106,168,255,.7);
    background: rgba(106,168,255,.12);
  }

  @media (max-width: 900px){
    .home-inner{ padding: 44px 16px; }
  }
</style>
