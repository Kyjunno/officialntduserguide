---
import BaseLayout from "../../layouts/BaseLayout.astro";

type UnitEntry = {
  name: string;
  rank: number;
  cost: number;
  maxDps: string;
  attackType: string;
  placements: string;
  stats: {
    damage: string;
    cooldown: string;
    range: string;
    dps: string;
  };
  image: string;
};

const units: UnitEntry[] = [
  { name: "Unit Name #1", rank: 1, cost: 800, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-1.png" },
  { name: "Unit Name #2", rank: 2, cost: 750, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-2.png" },
  { name: "Unit Name #3", rank: 3, cost: 700, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-3.png" },
  { name: "Unit Name #4", rank: 4, cost: 650, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-4.png" },
  { name: "Unit Name #5", rank: 5, cost: 600, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-5.png" },
  { name: "Unit Name #6", rank: 6, cost: 550, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-6.png" },
  { name: "Unit Name #7", rank: 7, cost: 500, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-7.png" },
  { name: "Unit Name #8", rank: 8, cost: 450, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-8.png" },
  { name: "Unit Name #9", rank: 9, cost: 400, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-9.png" },
  { name: "Unit Name #10", rank: 10, cost: 350, maxDps: "??", attackType: "??", placements: "??", stats: { damage: "??", cooldown: "??", range: "??", dps: "??" }, image: "/images/units/placeholders/unit-10.png" },
];
---
<BaseLayout title="Starter Tier List | Nightshift Tower Defense User Guide">
  <div class="tierpage-bg" aria-hidden="true"></div>
  <div class="tierpage-bg-overlay" aria-hidden="true"></div>

  <div class="page">
    <header class="hero">
      <h1 class="title">Starter Tier List</h1>
      <div class="sub">$800 and below</div>
      <div class="note">Reminder that this is made with my opinion in mind.</div>
    </header>

    <div class="layout">
      <!-- LEFT: the one big FNDT-style box -->
      <section class="leftPanel" aria-label="Top 10 starter units">
        <div class="leftHeader">
          <div class="leftTitle">Top 10 Starter Units</div>
          <div class="leftSub">Ranked list with quick stats</div>
        </div>

        <div class="rows">
          {units.map((u) => (
            <article class="row" aria-label={`${u.name} rank ${u.rank}`}>
              <div class="imgWrap">
                <img class="img" src={u.image} alt={u.name} loading="lazy" decoding="async" />
                <div class="tag name">{u.name}</div>
                <div class="tag rank">#{u.rank}</div>
                <div class="tag cost">${u.cost}</div>
              </div>

              <div class="info">
                <div class="miniGrid topLine">
                  <div class="miniBox">
                    <div class="k">Max DPS</div>
                    <div class="v">{u.maxDps}</div>
                  </div>
                  <div class="miniBox">
                    <div class="k">Attack</div>
                    <div class="v">{u.attackType}</div>
                  </div>
                  <div class="miniBox">
                    <div class="k">Places</div>
                    <div class="v">{u.placements}</div>
                  </div>
                </div>

                <div class="miniGrid statsLine">
                  <div class="miniBox">
                    <div class="k">DMG</div>
                    <div class="v">{u.stats.damage}</div>
                  </div>
                  <div class="miniBox">
                    <div class="k">CD</div>
                    <div class="v">{u.stats.cooldown}</div>
                  </div>
                  <div class="miniBox">
                    <div class="k">RNG</div>
                    <div class="v">{u.stats.range}</div>
                  </div>
                  <div class="miniBox">
                    <div class="k">DPS</div>
                    <div class="v">{u.stats.dps}</div>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
      </section>

      <!-- RIGHT: your commentary area (empty for now) -->
      <aside class="rightPanel" aria-label="Notes and explanations">
        <div class="rightHeader">
          <div class="rightTitle">Why these rankings?</div>
          <div class="rightSub">This is where your explanations will go.</div>
        </div>

        <div class="rightBody">
          <div class="hint">
            Add your reasoning here. You can do one paragraph per unit, or only explain the controversial picks.
          </div>
        </div>
      </aside>
    </div>
  </div>

  <style>
    /* Background */
    .tierpage-bg{
      position: fixed;
      inset: 0;
      z-index: -2;
      background-image: url("/images/tierlist-bg.png");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      filter: blur(1px) brightness(0.85) contrast(1.05) saturate(1.2);
      transform: scale(1.02);
    }
    .tierpage-bg-overlay{
      position: fixed;
      inset: 0;
      z-index: -1;
      background:
        radial-gradient(900px 650px at 20% 10%, rgba(106,168,255,.18), transparent 60%),
        radial-gradient(900px 650px at 80% 20%, rgba(159,91,255,.16), transparent 55%),
        linear-gradient(180deg, rgba(10,12,18,.22), rgba(10,12,18,.55));
    }

    /* Page */
    .page{
      max-width: 1200px;
      margin: 0 auto;
      padding: 26px 18px 80px;
    }

    .hero{
      text-align: center;
      margin-bottom: 18px;
    }

    .title{
      margin: 0;
      font-size: clamp(42px, 5vw, 66px);
      line-height: 1.02;
      letter-spacing: -0.02em;
    }

    .sub{
      margin-top: 10px;
      font-weight: 900;
      color: rgba(232,238,252,.9);
      letter-spacing: 0.02em;
      font-size: 14px;
    }

    .note{
      margin-top: 8px;
      color: rgba(232,238,252,.65);
      font-size: 13px;
      line-height: 1.55;
    }

    /* Layout: left big box + right notes */
    .layout{
      display: grid;
      grid-template-columns: 1.05fr 0.95fr;
      gap: 18px;
      align-items: start;
    }

    /* Left Panel (FNDT-style big box) */
    .leftPanel{
      border-radius: 22px;
      border: 1px solid rgba(255,255,255,.20);
      background: rgba(0,0,0,.22);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .leftHeader{
      padding: 14px 14px 10px;
      border-bottom: 1px solid rgba(255,255,255,.10);
    }

    .leftTitle{
      font-weight: 1000;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 13px;
      color: rgba(232,238,252,.95);
    }

    .leftSub{
      margin-top: 6px;
      font-size: 12px;
      color: rgba(232,238,252,.65);
    }

    .rows{
      padding: 12px;
      display: grid;
      gap: 10px;
    }

    /* Each unit row (compact) */
    .row{
      display: grid;
      grid-template-columns: 140px 1fr;
      gap: 10px;
      padding: 10px;
      border-radius: 18px;
      border: 1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.16);
    }

    /* Image card with overlays */
    .imgWrap{
      position: relative;
      border-radius: 16px;
      border: 2px solid rgba(232,238,252,.70);
      overflow: hidden;
      background: rgba(0,0,0,.22);
      aspect-ratio: 1 / 1;
    }

    .img{
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      display: block;
    }

    .tag{
      position: absolute;
      padding: 6px 8px;
      font-weight: 900;
      font-size: 11px;
      color: rgba(255,255,255,.92);
      background: rgba(0,0,0,.55);
      border: 1px solid rgba(255,255,255,.14);
      border-radius: 12px;
      backdrop-filter: blur(6px);
      line-height: 1;
    }

    .tag.name{
      top: 8px;
      left: 8px;
      max-width: calc(100% - 58px);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .tag.rank{
      top: 8px;
      right: 8px;
    }

    .tag.cost{
      bottom: 8px;
      left: 8px;
    }

    /* Compact info grid */
    .info{
      display: grid;
      gap: 8px;
      align-content: center;
    }

    .miniGrid{
      display: grid;
      gap: 8px;
    }

    .topLine{
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .statsLine{
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .miniBox{
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,.12);
      background: rgba(0,0,0,.14);
      padding: 8px 9px;
      min-height: 46px;
      display: grid;
      align-content: center;
      gap: 4px;
    }

    .k{
      font-size: 10px;
      font-weight: 900;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: rgba(232,238,252,.62);
    }

    .v{
      font-size: 12px;
      font-weight: 900;
      color: rgba(232,238,252,.92);
    }

    /* Right panel for your explanations */
    .rightPanel{
      border-radius: 22px;
      border: 1px solid rgba(255,255,255,.20);
      background: rgba(0,0,0,.18);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .rightHeader{
      padding: 14px 14px 10px;
      border-bottom: 1px solid rgba(255,255,255,.10);
    }

    .rightTitle{
      font-weight: 1000;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      font-size: 13px;
      color: rgba(232,238,252,.95);
    }

    .rightSub{
      margin-top: 6px;
      font-size: 12px;
      color: rgba(232,238,252,.65);
    }

    .rightBody{
      padding: 14px;
      color: rgba(232,238,252,.80);
      font-size: 13px;
      line-height: 1.7;
    }

    .hint{
      border-radius: 16px;
      border: 1px dashed rgba(255,255,255,.20);
      padding: 12px;
      background: rgba(0,0,0,.12);
      color: rgba(232,238,252,.72);
    }

    /* Responsive */
    @media (max-width: 1050px){
      .layout{
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 520px){
      .row{
        grid-template-columns: 1fr;
      }
      .topLine{
        grid-template-columns: 1fr;
      }
      .statsLine{
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }
  </style>
</BaseLayout>
