---
import BaseLayout from "../../layouts/BaseLayout.astro";

const pageTitle = "Boosters Tier List";
const pageSubtitle = "These are judged by power boost";
const pageNote = "Reminder that this is made with my opinion in mind.";

const units = [
  {
    rank: 1,
    name: "Endo Plush",
    image: "/images/tierlists/boosters/endo-plush.png",
    cost: "$500",
    rarity: "Epic",
    maxDps: "+15% POWER",
    attackType: "Boost + Reveal",
    placements: "3",
    dmg: "0",
    rng: "15",
    cd: "1",
    reasoning:
      "Endo plush and crying soul both give the same amount of power bonus, both are easy to get and overall do similar things. HOWEVER, endo plush ALSO has reveal, which in a game full of hidden enemies, is needed",
  },
  {
    rank: 2,
    name: "Crying Soul",
    image: "/images/tierlists/boosters/crying-soul.png",
    cost: "$999",
    rarity: "Mythic",
    maxDps: "+15% POWER",
    attackType: "Boost",
    placements: "3",
    dmg: "0",
    rng: "18",
    cd: "1",
    reasoning: "While crying soul gives the same amount of power as endo plush, crying soul doesn't reveal",
  },
  {
    rank: 3,
    name: "Bort Bunny",
    image: "/images/tierlists/boosters/bort-bunny.png",
    cost: "$2,500",
    rarity: "Mythic",
    maxDps: "180",
    attackType: "Pierce + Boost",
    placements: "2",
    dmg: "90",
    rng: "25",
    cd: "1",
    reasoning:
      "while yes, bort bunny does damage, his boost only increases POWER by 11.112%, combine with his price of $2,500 and you can see why he is third",
  },
  {
    rank: 4,
    name: "The Amalgamation",
    image: "/images/tierlists/boosters/the-amalgamation.png",
    cost: "$15,000",
    rarity: "Vengeful",
    maxDps: "625",
    attackType: "Cone AOE (50%)",
    placements: "1",
    dmg: "1250",
    rng: "30",
    cd: "2",
    reasoning:
      "While yes, he is the highest dps of all the boosters, we are looking at pure boost value. Out of all the boosters, his boost actually increase POWER the most, by 34.44% to be exact. So why is he 4th? WELL, that boost only last 10 seconds and has a cooldown of 60 seconds, making it a strong but unreliable boost",
  },
];
---

<BaseLayout title={`${pageTitle} | Nightshift Tower Defense User Guide`}>
  <!-- Full-page background (matches your other tier pages) -->
  <div class="tierpage-bg"></div>
  <div class="tierpage-bg-overlay"></div>

  <div class="tierpage">
    <header class="tierhero">
      <h1>{pageTitle}</h1>
      <p class="sub">{pageSubtitle}</p>
      <p class="note">{pageNote}</p>
    </header>

    <div class="tiergrid">
      <!-- LEFT: Top units panel -->
      <section class="tierpanel left">
        <div class="panelhead">
          <div class="paneltitle">Best Boosters</div>
          <div class="panelsub">Ranked by boost effectiveness</div>
        </div>

        <div class="unitlist">
          {units.map((u) => (
            <article class="unitrow">
              <div class="unitimg">
                <img src={u.image} alt={u.name} loading="lazy" />
                <div class="tag name">{u.name}</div>
                <div class="tag rank">#{u.rank}</div>
                <div class="tag cost">{u.cost}</div>
              </div>

              <div class="unitmeta">
                <div class="metagrid top">
                  <div class="metabox">
                    <div class="k">Max DPS</div>
                    <div class="v">{u.maxDps}</div>
                  </div>
                  <div class="metabox">
                    <div class="k">Attack Type</div>
                    <div class="v">{u.attackType}</div>
                  </div>
                  <div class="metabox">
                    <div class="k">Placements</div>
                    <div class="v">{u.placements}</div>
                  </div>
                </div>

                <div class="metagrid bottom">
                  <div class="metabox">
                    <div class="k">DMG</div>
                    <div class="v">{u.dmg}</div>
                  </div>
                  <div class="metabox">
                    <div class="k">CD</div>
                    <div class="v">{u.cd}</div>
                  </div>
                  <div class="metabox">
                    <div class="k">RNG</div>
                    <div class="v">{u.rng}</div>
                  </div>
                  <div class="metabox">
                    <div class="k">Rarity</div>
                    <div class="v">{u.rarity}</div>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
      </section>

      <!-- RIGHT: Why these rankings -->
      <aside class="tierpanel right">
        <div class="panelhead">
          <div class="paneltitle">Why these rankings?</div>
          <div class="panelsub">Per-unit reasoning</div>
        </div>

        <div class="reasonlist">
          {units.map((u) => (
            <div class="reasoncard">
              <div class="reasonhead">
                <div class="rname">{u.name}</div>
                <div class="rrank">#{u.rank}</div>
              </div>
              <div class="rtext">{u.reasoning}</div>
            </div>
          ))}
        </div>
      </aside>
    </div>
  </div>

  <style>
    /* Full-page background */
    .tierpage-bg {
      position: fixed;
      inset: 0;
      background: url("/images/tierlist-bg.png") center / cover no-repeat;
      filter: blur(1px) brightness(0.9);
      z-index: -2;
    }
    .tierpage-bg-overlay {
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(0,0,0,.25), rgba(0,0,0,.60));
      z-index: -1;
    }

    /* Page layout (this is what your screenshot is missing) */
    .tierpage {
      max-width: 1200px;
      margin: 0 auto;
      padding: 48px 20px 80px;
    }

    .tierhero {
      text-align: center;
      margin-bottom: 28px;
    }
    .tierhero h1 {
      font-size: clamp(34px, 4.2vw, 56px);
      line-height: 1.05;
      margin: 0 0 10px;
      font-weight: 800;
      letter-spacing: -0.02em;
    }
    .tierhero .sub {
      margin: 0;
      opacity: 0.9;
      font-size: 16px;
    }
    .tierhero .note {
      margin: 6px 0 0;
      opacity: 0.75;
      font-size: 14px;
    }

    .tiergrid {
      display: grid;
      grid-template-columns: 1.05fr 0.95fr;
      gap: 18px;
      align-items: start;
    }

    .tierpanel {
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(10,14,22,0.55);
      border-radius: 18px;
      padding: 16px;
      backdrop-filter: blur(10px);
    }

    .panelhead {
      padding: 4px 6px 12px;
      border-bottom: 1px solid rgba(255,255,255,0.10);
      margin-bottom: 14px;
    }
    .paneltitle {
      font-weight: 800;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      font-size: 13px;
      opacity: 0.95;
    }
    .panelsub {
      font-size: 13px;
      opacity: 0.75;
      margin-top: 4px;
    }

    .unitlist {
      display: grid;
      gap: 12px;
    }

    .unitrow {
      display: grid;
      grid-template-columns: 150px 1fr;
      gap: 12px;
      padding: 12px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(0,0,0,0.20);
    }

    .unitimg {
      position: relative;
      border-radius: 14px;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,0.18);
      background: rgba(0,0,0,0.35);
      height: 150px;
    }

    .unitimg img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .tag {
      position: absolute;
      padding: 6px 10px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 700;
      border: 1px solid rgba(255,255,255,0.14);
      background: rgba(0,0,0,0.45);
      backdrop-filter: blur(8px);
      white-space: nowrap;
    }
    .tag.name { top: 10px; left: 10px; max-width: 78%; overflow: hidden; text-overflow: ellipsis; }
    .tag.rank { top: 10px; right: 10px; }
    .tag.cost { bottom: 10px; left: 10px; }

    .unitmeta {
      display: grid;
      gap: 10px;
      align-content: center;
    }

    .metagrid {
      display: grid;
      gap: 10px;
    }
    .metagrid.top {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
    .metagrid.bottom {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .metabox {
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(0,0,0,0.22);
      border-radius: 12px;
      padding: 10px 10px;
      min-height: 54px;
      display: grid;
      align-content: center;
      gap: 4px;
    }
    .metabox .k {
      font-size: 11px;
      opacity: 0.72;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      font-weight: 700;
    }
    .metabox .v {
      font-size: 13px;
      font-weight: 800;
      opacity: 0.95;
    }

    .reasonlist {
      display: grid;
      gap: 12px;
    }

    .reasoncard {
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(0,0,0,0.18);
      border-radius: 14px;
      padding: 12px;
    }

    .reasonhead {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      align-items: center;
      margin-bottom: 8px;
    }

    .rname {
      font-weight: 900;
      font-size: 14px;
    }
    .rrank {
      font-weight: 900;
      font-size: 13px;
      opacity: 0.8;
    }

    .rtext {
      font-size: 13px;
      line-height: 1.45;
      opacity: 0.92;
    }

    @media (max-width: 980px) {
      .tiergrid {
        grid-template-columns: 1fr;
      }
      .unitrow {
        grid-template-columns: 140px 1fr;
      }
      .unitimg {
        height: 140px;
      }
    }

    @media (max-width: 520px) {
      .unitrow {
        grid-template-columns: 1fr;
      }
      .unitimg {
        height: 210px;
      }
      .metagrid.top {
        grid-template-columns: 1fr;
      }
      .metagrid.bottom {
        grid-template-columns: 1fr 1fr;
      }
    }
  </style>
</BaseLayout>
